meta:
    environment: node
    toolchain:
        name: npm
        version: 18

    environment: db
    toolchain:
      name: postgresql
      version: 13

build:
    additionalCommands: npm run build

run:
    command: npm run start
    args: listen_addresses=0.0.0.0
